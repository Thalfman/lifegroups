# Example case — input

Replace with a real task for the agent. Keep it self-contained.

> Use relative dates ("today", "since yesterday") so the case stays valid over
> time.

Example:
"Review the staged diff and report any change that lacks a corresponding test,
one finding per line as `path:line — issue`."
