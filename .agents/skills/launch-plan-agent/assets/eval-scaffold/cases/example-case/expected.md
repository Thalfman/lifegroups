# Example case — expected (rubric)

What a good answer MUST contain. Used to grade the run, not string-matched.

- [ ] {{Must mention X}}
- [ ] {{Format is `path:line — issue`}}
- [ ] {{No false positives on Y}}
- [ ] {{Returns "none" cleanly when there's nothing to report}}
