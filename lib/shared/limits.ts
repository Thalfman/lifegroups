// Shared free-text length bounds. Client `maxLength` attributes and the
// server validators must agree — both sides import this constant so the bound
// can't drift between the textarea and the validation error.

// Notes / summaries / next-step detail across care, follow-ups, prospects,
// launch planning, and the apprentice pipeline.
export const NOTE_MAX_CHARS = 2000;
