node_modules
.next
out

# Playwright
/test-results
/playwright-report
/blob-report
/playwright/.cache

.env*
!.env.example
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store
tsconfig.tsbuildinfo

# Local-only dev helpers (do not commit real auth user UUIDs).
supabase/dev/link_test_users.sql
supabase/dev/link_super_admin.sql

# Claude Code agent worktrees (transient, isolated)
.claude/worktrees/

# Graphify knowledge graph: graph + report are committed; the extraction
# cache and manifest are machine-local (absolute paths / mtimes) and are not.
/graphify-out/cache/
/graphify-out/manifest.json
/graphify-out/.update.log
