#!/bin/sh
# Diagnose a missing/partial local install up front (#545), before lint-staged
# can fail with bare shell noise. typecheck/test:run preflight again themselves.
npm run verify:toolchain
npx lint-staged
npm run typecheck
npm run test:run
